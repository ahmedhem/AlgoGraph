// TODO: Round 1 >> create the button >> make it absolute >> position it to the right-bottom of the page (done) >> make the button draggable to any place else in the screen (done) >>add the style(color, rounded, hover, ...) and the icon inside the button (done)
// TODO: add the feature to open and collapse the toolBox on click

import dragElement from "./DragElement.js";

dragElement(document.getElementById("toolbox-toggle"));

