// TODO: Round 1
// TODO: create the button >> make it absolute >> position it to the left-bottom of the page
// TODO: make the button draggable to any place else in the screen
// TODO: add the style(color, rounded, hover, ...) and the icon inside the button
// TODO: add the feature to open and collapse the toolBox on click